apply from: new File(rootDir, "/core/AppConfig/compose_application.gradle")

def plugins = rootProject.ext.plugins
def cfg = rootProject.ext.android
def libs = rootProject.ext.dependencies

android {
}

dependencies {
}